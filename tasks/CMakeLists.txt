# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(aruco_marker_conversionTaskLib)
ADD_LIBRARY(${ARUCO_MARKER_CONVERSION_TASKLIB_NAME} SHARED 
    ${ARUCO_MARKER_CONVERSION_TASKLIB_SOURCES})
add_dependencies(${ARUCO_MARKER_CONVERSION_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${ARUCO_MARKER_CONVERSION_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${ARUCO_MARKER_CONVERSION_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${ARUCO_MARKER_CONVERSION_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${ARUCO_MARKER_CONVERSION_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${ARUCO_MARKER_CONVERSION_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${ARUCO_MARKER_CONVERSION_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${ARUCO_MARKER_CONVERSION_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${ARUCO_MARKER_CONVERSION_TASKLIB_HEADERS}
    DESTINATION include/orocos/aruco_marker_conversion)

